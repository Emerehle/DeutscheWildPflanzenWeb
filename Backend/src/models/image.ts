export interface ImageMeta {
  pflanze_id: number;
  original_url: string;
  cloudinary_url: string;
  titel: string;
  urheber: string;
  lizenz: string;
  lizenz_url: string;
  attribution: string;
  beschreibung: string;
  datum: string;
  originalseite_url: string;
  erstellt_am?: Date;
}
